pipeline {
    agent any 

    stages {
        stage ("Dep install"){
            agent {docker {image "nodejs}}
            steps{
                sh "set -x"  // Enable debug mode
                sh "pwd"
                sh "ls -l"
                sh 'sudo chown -R 995:993 "/.npm"'
                sh "npm install"
            }
        }
    }
}