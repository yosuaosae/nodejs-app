pipeline {
    agent any 

    stages {
        stage ("Dep install"){
            agent {docker {image "node:14"}}
            steps{
                sh "set -x"  // Enable debug mode
                sh 'sudo chown -R 995:993"/.npm'
                sh "npm install"
            }
        }
    }
}